syntax = "proto3";
import "nanopb.proto";

package HT_Etherbuf;

message CANWrapper {
    message CANMessage{
        optional int32 id = 1;
        repeated bytes data = 2 [(nanopb).max_size = 64, (nanopb).max_count = 8, (nanopb).fixed_length = true];
    }

    repeated CANMessage messages = 1 [(nanopb).max_size = 680, (nanopb).max_count = 10, (nanopb).fixed_length = true];
    optional int64 timestamp = 2;
}